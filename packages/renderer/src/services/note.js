// import Base from './base';

// class Note extends Base {
//   _table = 'notes';
// }

// export default new Note({ id: { type: 'string', pk: true } });

import Model from './base';

export default new Model('notes');
