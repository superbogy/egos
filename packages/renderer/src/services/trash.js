// import Base from './base';

// class Trash extends Base {
//   _table = 'trash';
// }
// const schema = { id: { type: 'string', pk: true } };
// export default new Trash(schema);

import Model from './base';

export default new Model('trash');
