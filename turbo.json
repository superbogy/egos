{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "compile": {
      "dependsOn": ["build"]
    },
    "build": {
      "outputs": [".next/**"]
    },
    "lint": {
      "outputs": []
    }
  }
}
